var CommonData = {
	'userId': WebTools.getCookie('userId') ? WebTools.getCookie('userId') : '1',
	'token':  WebTools.getCookie('token') ? WebTools.getCookie('token') : ''
};

var ApiConfig = {
	'home': 'json/home.json'
};